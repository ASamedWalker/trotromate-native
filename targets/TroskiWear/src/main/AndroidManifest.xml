<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.troski.wear">

    <!-- Mark as Wear OS standalone app -->
    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Wearable Data Layer API -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:label="Troski"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tile Service -->
        <service
            android:name=".tile.TroskiTileService"
            android:exported="true"
            android:label="Troski Commute"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>

            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview" />
        </service>

        <!-- Wearable data listener service for background data sync -->
        <service
            android:name=".data.WearDataListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/commute"
                    android:scheme="wear" />
                <data
                    android:host="*"
                    android:pathPrefix="/stations"
                    android:scheme="wear" />
                <data
                    android:host="*"
                    android:pathPrefix="/alert"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <!-- Required for Google Play Services wearable -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

    </application>
</manifest>
